import fs from'node:fs';import path from'node:path';const root=process.cwd(),walk=d=>fs.readdirSync(d,{withFileTypes:true}).flatMap(x=>x.name==='.git'?[]:x.isDirectory()?walk(path.join(d,x.name)):[path.join(d,x.name)]);for(const f of walk(root).filter(x=>x.endsWith('.html'))){let s=fs.readFileSync(f,'utf8'),rel=path.relative(root,f).replaceAll('\\','/'),prefix=rel.includes('/')?'../':'';if(!s.includes('pressdesk.css'))s=s.replace(/<\/head>/i,`<link rel="stylesheet" href="${prefix}assets/css/pressdesk.css"></head>`);s=s.replace('<body>','<body class="pressdesk">');fs.writeFileSync(f,s)}
