import fs from 'node:fs';import path from 'node:path';import{fileURLToPath}from'node:url';const root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'..');const walk=d=>fs.readdirSync(d,{withFileTypes:true}).flatMap(x=>x.name==='.git'?[]:x.isDirectory()?walk(path.join(d,x.name)):x.name.endsWith('.html')?[path.join(d,x.name)]:[]);for(const f of walk(root)){let s=fs.readFileSync(f,'utf8'),rel=path.relative(root,f).replaceAll('\\','/'),prefix=rel.includes('/')?'../':'';if(!s.includes('polish.css'))s=s.replace(/<\/head>/i,`<link rel="stylesheet" href="${prefix}assets/css/polish.css"><link rel="icon" href="${prefix}favicon.svg" type="image/svg+xml"></head>`);fs.writeFileSync(f,s)}
