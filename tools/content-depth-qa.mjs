import fs from 'node:fs';
const read = path => fs.readFileSync(path, 'utf8');
const words = html => html.replace(/<script[\s\S]*?<\/script>/gi, '').replace(/<[^>]*>/g, ' ').replace(/\s+/g, ' ').trim().split(' ').filter(Boolean).length;
const duplicateParagraphs = new Map(), bad = [];
const calculatorFiles = fs.readdirSync('tools').filter(f => f.endsWith('.html') && /class="[^"]*\bcalculator\b[^"]*"/.test(read(`tools/${f}`)));
const articleFiles = [...fs.readdirSync('guides').filter(f => f.endsWith('.html') && f !== 'index.html').map(f => `guides/${f}`), ...fs.readdirSync('reference').filter(f => f.endsWith('.html') && f !== 'index.html').map(f => `reference/${f}`)];
const calculatorNeed = ['What this calculator solves', 'How to choose the inputs', 'How the calculation works', 'Worked production example', 'How to interpret the result', 'Common production mistakes', 'Production assumptions and limitations', 'Related production workflow'];
const postPressNeed = ['What this calculator solves', 'When to use it and how to choose inputs', 'Calculation method', 'Worked production example', 'Interpretation and common mistakes', 'Assumptions and limitations'];
const webHandlingNeed = ['What this', 'Inputs and units', 'Calculation method', 'Worked production example', 'How to use the result', 'Common production mistakes', 'Assumptions and limitations', 'Related workflow and sources'];
for (const file of calculatorFiles) { const path = `tools/${file}`, source = read(path), isPostPress = source.includes('data-postpress='), isWebHandling = source.includes('runWebHandling('); const required = isPostPress ? postPressNeed : isWebHandling ? webHandlingNeed : calculatorNeed; for (const heading of required) if (!source.includes(heading)) bad.push(`${path}: missing ${heading}`); const target = isPostPress || isWebHandling ? 350 : 550; if (words(source) < target) bad.push(`${path}: short body`); }
for (const path of articleFiles) { const source = read(path), isPostPress = path === 'guides/post-press-preflight.html'; const required = isPostPress ? ['Start with the physical job', 'Folding and creasing', 'Lamination', 'Cutting, spoilage and handoff', 'Preflight checklist'] : ['Purpose and production context', 'Definitions and distinctions', 'Inputs, units and method', 'Worked production example', 'Decision checklist', 'Common mistakes and cautions', 'Related workflow']; for (const heading of required) if (!source.includes(heading)) bad.push(`${path}: missing ${heading}`); if (!/Last reviewed: 2026-\d{2}-\d{2}/.test(source)) bad.push(`${path}: missing review date`); const target = isPostPress ? 330 : path.startsWith('guides/') ? 700 : 550; if (words(source) < target) bad.push(`${path}: short body`); }
for (const path of [...calculatorFiles.map(f => `tools/${f}`), ...articleFiles]) for (const paragraph of [...read(path).matchAll(/<p[^>]*>([\s\S]*?)<\/p>/gi)].map(m => m[1].replace(/<[^>]*>/g, ' ').replace(/\s+/g, ' ').trim()).filter(p => p.split(' ').length >= 18)) duplicateParagraphs.set(paragraph, [...(duplicateParagraphs.get(paragraph) || []), path]);
for (const [paragraph, paths] of duplicateParagraphs) if (paths.length > 1) bad.push(`duplicate long paragraph: ${paragraph.slice(0, 140)} — ${paths.join(', ')}`);
if (bad.length) { console.error(bad.join('\n')); process.exit(1); }
console.log(`Content QA PASS: ${calculatorFiles.length} calculators and ${articleFiles.length} articles`);
