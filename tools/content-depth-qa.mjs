import fs from'node:fs';const files=fs.readdirSync('tools').filter(f=>f.endsWith('.html')&&f!=='index.html'),need=['When to use this calculator','Inputs explained','Worked example','Production considerations','Common mistakes','Related production content'],bad=[];for(const f of files){const s=fs.readFileSync('tools/'+f,'utf8');if(!s.includes('calculator'))continue;for(const n of need)if(!s.includes(n))bad.push(`${f}: ${n}`);if(s.replace(/<[^>]*>/g,'').length<1400)bad.push(`${f}: short body`)}if(bad.length){console.error(bad.join('\n'));process.exit(1)}console.log(`Content depth QA PASS: ${files.length} calculator pages`);
